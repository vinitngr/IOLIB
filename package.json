{
  "name": "iolib",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build" : "npm i && npm run build --prefix client && npm run build --prefix server",
    "start": "npm run start --prefix server"
  },
  "keywords": ["hackathon" , "langchain" , "ai"],
  "author": "vinit nagar",
  "license": "ISC",
  "description": "IOLIB a ai library based on RAG"
}
